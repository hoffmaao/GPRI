#!/usr/bin/env python3
"""Entry point for the gpri command line tool."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from gpri.cli import main

if __name__ == "__main__":
    sys.exit(main())
