# staged data and processing products -- never commit, these are 10s of GB
work/
*.slc
*.mli
*.int
*.cc
*.off
*.bmp
*.ave
*.npz

# GeoTIFFs are outputs, except the small ones committed under docs/figures
*.tif
!docs/figures/*.tif

# python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/

# macOS turds (the source data trees are full of them)
.DS_Store
._*

# local data caches (RGI outlines, DEMs, ...)
data/

# site-local paths and notes -- machine details never go in the repository
site.env
docs/local/
